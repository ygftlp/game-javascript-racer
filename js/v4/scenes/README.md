# Scenes

Future scene modules should live here.

Planned flow:

```text
boot -> menu -> countdown -> playing -> paused -> result
```

Keep this folder empty of runtime code until the scene manager is introduced. The current game still starts directly through `js/v4/game.js`.
